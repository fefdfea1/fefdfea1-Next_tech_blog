import { styled } from "@/styled-system/jsx";
export default function ExtensionPost() {
  return <ExtensionPostContainer />;
}

const ExtensionPostContainer = styled("article", {
  base: {
    width: "100%",
    maxWidth: "",
  },
});
